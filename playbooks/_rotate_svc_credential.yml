---
# Included by rotate_svc_credentials.yml for each team.
# Generates a new password and updates both the user and credential.

- name: "Set team facts for {{ team_var_name }}"
  ansible.builtin.set_fact:
    _team: "{{ lookup('vars', team_var_name) }}"

- name: "Generate new password for svc-{{ _team.team_name }}-cac"
  ansible.builtin.set_fact:
    _team_svc_password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits,punctuation') }}"
  no_log: true

- name: "Update service account password: svc-{{ _team.team_name }}-cac"
  ansible.platform.user:
    username: "svc-{{ _team.team_name }}-cac"
    password: "{{ _team_svc_password }}"
    state: present
  no_log: true

- name: "Update AAP gateway credential: {{ _team.team_name }}-gateway-cred"
  ansible.controller.credential:
    name: "{{ _team.team_name }}-gateway-cred"
    organization: "Platform"
    credential_type: "AAP CaC Credential"
    inputs:
      host: "{{ aap_hostname }}"
      username: "svc-{{ _team.team_name }}-cac"
      password: "{{ _team_svc_password }}"
      verify_ssl: "{{ aap_validate_certs }}"
    state: present
    controller_host: "{{ aap_hostname }}"
    controller_username: "{{ aap_username }}"
    controller_password: "{{ aap_password }}"
    validate_certs: "{{ aap_validate_certs }}"
  no_log: true
