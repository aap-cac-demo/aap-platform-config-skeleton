---
controller_inventories_all:
  - name: cac-localhost
    description: "Localhost inventory for platform CaC execution"
    organization: Platform
    state: present

controller_hosts_all:
  - name: localhost
    inventory: cac-localhost
    variables:
      ansible_connection: local
      ansible_python_interpreter: "{{ '{{' }} ansible_playbook_python {{ '}}' }}"
    state: present
...
